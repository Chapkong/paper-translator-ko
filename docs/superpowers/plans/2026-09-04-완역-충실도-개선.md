# 완역 충실도 개선 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 저널 2단 PDF를 원문 손실 없이 추출하고, 손실이 남으면 번역 전에 멈추며, 조립 후 문서 전체를 통독해 문맥·역어를 바로잡는다.

**Architecture:** 레이아웃 재구성은 KorDocAI CLI(`npx kordoc --no-tables`)에 맡긴다(실측 보존율 0.972 대 현행 0.686). KorDocAI가 남기는 문제 — 한 줄이 한 문단, 하이픈 분철, 전면 스캔 이미지 — 만 `post.py`가 후처리하며, 이때 PyMuPDF 블록을 **문단 경계와 폰트 크기의 정답지**로 쓴다. `quality.py`가 결과를 채점해 미달이면 기존 pymupdf4llm 경로로 재시도하고 그래도 미달이면 파이프라인을 중단한다.

**Tech Stack:** Python 3, KorDocAI CLI(npm `kordoc`), PyMuPDF 1.28, 의존성 없는 자체 테스트 러너. 새 Python 패키지 추가 없음.

## Global Constraints

- 원문 충실도가 최우선이다. 속도·비용보다 앞선다. 원문에 없는 내용을 추론해 채우지 않는다.
- 출력 계약 불변: `work/<stem>/{source.md, images/, meta.json}`. `chunk.py`·`assemble.py`·`to_html.py`의 인터페이스는 바뀌지 않는다.
- KorDocAI 호출은 반드시 `--no-tables`를 붙인다. 기본값에서는 2단 조판의 테두리를 표로 오인해 좌우 단이 한 줄에 섞인다(도구 문서의 `#64`).
- KorDocAI는 CLI로만 부른다. MCP로 부르면 결과가 대화 컨텍스트를 통과해 논문 1편당 2만 토큰 이상을 쓴다.
- 손실 게이트 기준: 문자 보존율 ≥ 0.97, 잘린 문단 비율 ≤ 3%. 두 값은 `quality.py` 상수와 `명령서.md`에 같은 값으로 적는다.
- 각주 폰트 판정: 본문 폰트 중앙값의 0.85배 이하. 폰트 크기를 모르는 줄은 **본문으로 간주한다**(각주로 오판해 본문을 인용 블록에 가두는 쪽이 더 나쁘다).
- 이미지 채택 범위: 페이지 면적의 3%~60%. 여러 페이지에 반복 등장하는 xref와 표지 페이지 이미지는 제외한다. (실측: 로고 0.62%·1.71%, 전면 스캔 91.32%, sample.pdf 실제 그림 12.99%)
- 모든 파일 입출력에 `encoding="utf-8"`을 명시한다. 한글 경로에서 cp949로 저장되면 안 된다.
- 테스트는 `python scripts/selftest.py [이름 ...]`으로 실행한다. pytest를 도입하지 않는다.
- 커밋 메시지는 한국어로 쓰고 마지막 줄에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`를 넣는다.

---

### Task 1: 테스트 러너와 합성 PDF 픽스처

**Files:**
- Create: `scripts/selftest.py`

**Interfaces:**
- Produces: `test(fn)` 데코레이터로 테스트 등록, `build_fixture() -> Path`가 3쪽 2단 PDF를 만들어 경로를 돌려준다. `ROOT`(저장소 루트 Path)도 함께 노출한다. 이후 모든 태스크가 이 셋을 쓴다.

픽스처는 **정답을 아는 PDF**다. 실제 논문으로는 무엇이 정답인지 사람이 매번 눈으로 봐야 하지만, 우리가 그린 PDF는 원래 문장을 알고 있으므로 후처리 결과와 정확히 비교할 수 있다.

- [ ] **Step 1: 러너와 픽스처를 작성한다**

```python
#!/usr/bin/env python3
"""의존성 없는 자체 테스트.

사용법:
    python scripts/selftest.py            # 전체 실행
    python scripts/selftest.py 이름 이름   # 지정한 테스트만 실행
"""
import sys, traceback
from pathlib import Path

import pymupdf

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(Path(__file__).resolve().parent))

TESTS = {}


def test(fn):
    TESTS[fn.__name__] = fn
    return fn


FIXTURE = ROOT / "work" / "_fixture" / "fixture.pdf"

# 픽스처 본문 — 후처리 결과가 이 문장들과 정확히 일치해야 한다
P1_LEFT = ["INTRODUCTION",
           "The model is developed in this",
           "section. Firms rely on communi-",
           "cation among their units."]
P1_RIGHT = ["This right column follows the",
            "left one in reading order."]
P2_LEFT = ["Heterogeneity is the first",
           "condition of the model."]
P2_RIGHT = ["Rents are bound to the firm",
            "under imperfect mobility."]
P2_NOTE = ["1 See Nelson (1991) for a",
           "discussion of firm capabilities."]
P3_LEFT = ["The final section applies the",
           "model to strategy formulation."]
P3_RIGHT = ["Implications for research are",
            "discussed at the end."]


def _draw(page, x0, y0, lines, size):
    """줄바꿈 위치를 우리가 정하기 위해 한 줄씩 직접 찍는다."""
    for i, line in enumerate(lines):
        page.insert_text((x0, y0 + i * (size + 3)), line, fontsize=size, fontname="helv")


def build_fixture() -> Path:
    """2단 본문 + 각주 + 러닝헤드 + 반복 푸터 + 그림/로고가 든 3쪽 PDF를 만든다."""
    FIXTURE.parent.mkdir(parents=True, exist_ok=True)
    doc = pymupdf.open()
    body = [(P1_LEFT, P1_RIGHT), (P2_LEFT, P2_RIGHT), (P3_LEFT, P3_RIGHT)]
    for pno, (left, right) in enumerate(body):
        page = doc.new_page(width=530, height=800)
        _draw(page, 48, 45, [f"{180 + pno} A. Author"], 9.8)      # 러닝헤드
        _draw(page, 48, 80, left, 8.0)
        _draw(page, 285, 80, right, 8.0)
        if pno == 1:
            _draw(page, 285, 620, P2_NOTE, 6.5)                    # 각주
        _draw(page, 120, 750, ["This content downloaded on Tue, 01 Sep 2026"], 8.0)
    page = doc[2]                                                   # 그림 7% + 로고 0.4%
    fig = pymupdf.Pixmap(pymupdf.csRGB, pymupdf.IRect(0, 0, 200, 150))
    fig.set_rect(fig.irect, (30, 90, 200))
    page.insert_image(pymupdf.Rect(60, 300, 260, 450), pixmap=fig)
    logo = pymupdf.Pixmap(pymupdf.csRGB, pymupdf.IRect(0, 0, 40, 40))
    logo.set_rect(logo.irect, (200, 30, 30))
    page.insert_image(pymupdf.Rect(300, 300, 340, 340), pixmap=logo)
    doc.save(FIXTURE)
    doc.close()
    return FIXTURE


@test
def fixture_builds():
    p = build_fixture()
    doc = pymupdf.open(p)
    assert doc.page_count == 3, doc.page_count
    text = doc[0].get_text()
    assert "INTRODUCTION" in text
    assert "communi-" in text, "하이픈 분철이 픽스처에 들어가야 한다"
    doc.close()


def main():
    names = sys.argv[1:] or list(TESTS)
    unknown = [n for n in names if n not in TESTS]
    if unknown:
        sys.exit(f"없는 테스트: {unknown}\n가능: {list(TESTS)}")
    failed = []
    for n in names:
        try:
            TESTS[n]()
            print(f"ok   {n}")
        except Exception:
            failed.append(n)
            print(f"FAIL {n}")
            traceback.print_exc()
    print(f"\n{len(names) - len(failed)}/{len(names)} 통과")
    sys.exit(1 if failed else 0)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행해서 통과하는지 본다**

Run: `python scripts/selftest.py fixture_builds`
Expected: `ok   fixture_builds`, `1/1 통과`

- [ ] **Step 3: 커밋**

```bash
git add scripts/selftest.py
git commit -m "$(printf '테스트: 자체 러너와 합성 2단 PDF 픽스처 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 2: KorDocAI CLI 어댑터

**Files:**
- Create: `scripts/kordoc.py`
- Modify: `scripts/selftest.py`

**Interfaces:**
- Produces: `available() -> bool`, `to_markdown(src: Path, timeout=600) -> str | None` — CLI가 없거나 실패하면 `None`을 돌려준다(예외를 던지지 않는다. 호출부가 폴백을 결정한다).

CLI 자체는 우리 코드가 아니므로 출력 품질을 테스트하지 않는다. **부재를 감지해 `None`을 돌려주는지만** 확인한다. 그래야 오프라인에서도 파이프라인이 폴백으로 살아난다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def kordoc_adapter_degrades_gracefully():
    import kordoc
    assert kordoc.to_markdown(ROOT / "input" / "없는파일.pdf") is None, \
        "실패 시 예외가 아니라 None이어야 폴백할 수 있다"
    if kordoc.available():          # CLI가 있는 환경에서만 본 검증
        md = kordoc.to_markdown(build_fixture())
        assert md and "INTRODUCTION" in md, (md or "")[:200]
    else:
        print("     (kordoc CLI 없음 — 폴백 경로만 검증)")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py kordoc_adapter_degrades_gracefully`
Expected: FAIL — `ModuleNotFoundError: No module named 'kordoc'`

- [ ] **Step 3: 구현을 쓴다**

```python
#!/usr/bin/env python3
"""KorDocAI CLI 어댑터.

레이아웃 재구성(2단 읽기 순서, 머리글·바닥글 제거)을 CLI에 맡긴다.
실측: Peteraf 논문 보존율 0.972 (pymupdf4llm 0.686), JSTOR 푸터 14회 → 0회.

MCP가 아니라 CLI로 부른다. MCP 결과는 대화 컨텍스트를 통과해 논문 1편당
2만 토큰 이상을 쓰지만, CLI는 토큰이 들지 않고 재현 가능하다.
"""
import shutil, subprocess, sys, tempfile
from pathlib import Path

NPX = shutil.which("npx.cmd") or shutil.which("npx")


def available() -> bool:
    return NPX is not None


def to_markdown(src: Path, timeout: int = 600):
    """PDF를 마크다운 문자열로 돌려준다. 실패하면 None."""
    if not available() or not Path(src).exists():
        return None
    with tempfile.TemporaryDirectory() as td:
        out = Path(td) / "out.md"
        cmd = [NPX, "-y", "kordoc", str(src), "--no-tables", "--silent", "-o", str(out)]
        try:
            r = subprocess.run(cmd, capture_output=True, text=True,
                               encoding="utf-8", errors="replace", timeout=timeout)
        except (subprocess.TimeoutExpired, OSError) as e:
            print(f"kordoc 실행 실패: {e}", file=sys.stderr)
            return None
        if r.returncode != 0 or not out.exists():
            print(f"kordoc 실패(코드 {r.returncode}): {(r.stderr or '')[:300]}", file=sys.stderr)
            return None
        return out.read_text(encoding="utf-8")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `2/2 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/kordoc.py scripts/selftest.py
git commit -m "$(printf '추출: KorDocAI CLI 어댑터 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 3: PyMuPDF 정답지 — 문단 경계와 폰트 크기

**Files:**
- Create: `scripts/post.py`
- Modify: `scripts/selftest.py`

**Interfaces:**
- Produces: `key(text) -> str` (줄 대조용 정규화 키), `Oracle` 데이터클래스(필드: `starts: set[str]`, `sizes: dict[str, float]`, `body_size: float`, `page_last: dict[int, str]`), `build_oracle(doc) -> Oracle`

KorDocAI 출력에는 문단 경계도 폰트 크기도 없다. PyMuPDF 블록이 그 둘을 알고 있으므로 **정답지로만** 쓴다. 레이아웃 판단은 하지 않는다 — 그건 KorDocAI가 이미 했다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def oracle_knows_paragraph_starts_and_sizes():
    from post import build_oracle, key
    doc = pymupdf.open(build_fixture())
    o = build_oracle(doc)
    assert key("The model is developed in this") in o.starts, "블록 첫 줄은 문단 시작이다"
    assert key("section. Firms rely on communi-") not in o.starts, "이어지는 줄은 문단 시작이 아니다"
    assert 7.5 < o.body_size < 8.5, o.body_size
    assert o.sizes[key("1 See Nelson (1991) for a")] < o.body_size * 0.85, "각주는 작은 폰트다"
    assert o.page_last[2], "페이지별 마지막 줄이 있어야 이미지를 그 자리에 넣는다"
    doc.close()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py oracle_knows_paragraph_starts_and_sizes`
Expected: FAIL — `ModuleNotFoundError: No module named 'post'`

- [ ] **Step 3: 구현을 쓴다**

```python
#!/usr/bin/env python3
"""KorDocAI 출력 후처리.

KorDocAI는 읽기 순서와 머리글·바닥글을 해결해 주지만 한 줄을 한 문단으로 내놓는다.
문단 경계·폰트 크기는 PyMuPDF 블록에서 정답지를 만들어 복원한다.
"""
import re, statistics
from collections import defaultdict
from dataclasses import dataclass, field


def key(text: str) -> str:
    """줄 대조용 키. 공백·구두점 차이를 무시한다."""
    return re.sub(r"\W+", "", text).lower()[:40]


@dataclass
class Oracle:
    starts: set = field(default_factory=set)      # 문단 첫 줄 키
    sizes: dict = field(default_factory=dict)     # 줄 키 → 폰트 크기
    body_size: float = 0.0                        # 본문 폰트 중앙값
    page_last: dict = field(default_factory=dict) # 페이지 → 마지막 줄 키


def build_oracle(doc) -> Oracle:
    o = Oracle()
    all_sizes = []
    for pno in range(doc.page_count):
        last = ""
        for b in doc[pno].get_text("dict")["blocks"]:
            if b.get("type") != 0:
                continue
            first = True
            for ln in b["lines"]:
                txt = "".join(sp["text"] for sp in ln["spans"]).strip()
                if not txt:
                    continue
                k = key(txt)
                if not k:
                    continue
                if first:
                    o.starts.add(k)
                    first = False
                sizes = [sp["size"] for sp in ln["spans"] if sp["size"] >= 2.0]
                if sizes:
                    med = statistics.median(sizes)
                    o.sizes.setdefault(k, med)
                    all_sizes.append(med)
                last = k
        if last:
            o.page_last[pno] = last
    o.body_size = statistics.median(all_sizes) if all_sizes else 0.0
    return o
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `3/3 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/post.py scripts/selftest.py
git commit -m "$(printf '후처리: PyMuPDF 문단 경계·폰트 크기 정답지 생성\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 4: 문단 복원과 하이픈 결합

**Files:**
- Modify: `scripts/post.py`, `scripts/selftest.py`

**Interfaces:**
- Consumes: `Oracle`, `key`
- Produces: `units(md) -> list[str]` (KorDocAI 출력을 줄 단위로 분해), `join_unit(acc, part) -> str` (하이픈 결합 규칙), `rebuild_paragraphs(md, oracle) -> list[tuple[str, str]]` — **(첫 줄 키, 문단 텍스트)** 쌍

문단은 자기 첫 줄의 키를 들고 다녀야 한다. 정답지의 폰트 크기는 줄 단위로 저장돼 있어서, 줄을 합친 뒤 문단 전체 텍스트로 조회하면 키가 달라져 각주 판정이 항상 실패한다.

`communi-` + `cation` → `communication`. 현재 Peteraf 출력에 61곳 남아 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def paragraphs_and_hyphens_are_rebuilt():
    from post import rebuild_paragraphs, join_unit, Oracle, key
    o = Oracle(starts={key("The model is developed in this"), key("Rents are bound to the firm")},
               sizes={}, body_size=8.0)
    md = ("The model is developed in this\n\nsection. Firms rely on communi-\n\n"
          "cation among their units.\n\nRents are bound to the firm\n\nunder imperfect mobility.")
    paras = rebuild_paragraphs(md, o)
    assert [p[1] for p in paras] == [
        "The model is developed in this section. Firms rely on communication among their units.",
        "Rents are bound to the firm under imperfect mobility."], paras
    assert paras[0][0] == key("The model is developed in this"), "문단은 첫 줄 키를 들고 다녀야 한다"
    assert join_unit("ends with communi-", "cation") == "ends with communication"
    assert join_unit("ends with word", "next") == "ends with word next"
    assert join_unit("", "first") == "first"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py paragraphs_and_hyphens_are_rebuilt`
Expected: FAIL — `ImportError: cannot import name 'rebuild_paragraphs'`

- [ ] **Step 3: 구현을 추가한다**

```python
def units(md: str) -> list[str]:
    """KorDocAI 출력은 PDF 한 줄이 한 문단이다. 빈 줄 기준으로 쪼갠다."""
    return [u.strip() for u in md.split("\n\n") if u.strip()]


def join_unit(acc: str, part: str) -> str:
    """줄을 잇는다. 줄 끝 하이픈은 분철이므로 공백 없이 붙인다."""
    if not acc:
        return part
    if re.search(r"\w-$", acc):
        return acc[:-1] + part
    return acc + " " + part


def rebuild_paragraphs(md: str, oracle) -> list:
    """정답지의 문단 시작 줄에서만 새 문단을 연다.

    반환값은 (첫 줄 키, 문단 텍스트) 쌍이다. 폰트 크기 정답지가 줄 단위이므로
    각주를 판정하려면 문단이 자기 첫 줄의 키를 기억하고 있어야 한다.
    """
    paras, cur, k0 = [], "", ""
    for u in units(md):
        starts_new = (not cur) or key(u) in oracle.starts or u.startswith(("#", "!", "|", ">"))
        if starts_new:
            if cur:
                paras.append((k0, cur))
            cur, k0 = u, key(u)
        else:
            cur = join_unit(cur, u)
    if cur:
        paras.append((k0, cur))
    return paras
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `4/4 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/post.py scripts/selftest.py
git commit -m "$(printf '후처리: 문단 복원과 하이픈 분철 결합\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 5: 각주 표시와 표지 배너 제거

**Files:**
- Modify: `scripts/post.py`, `scripts/selftest.py`

**Interfaces:**
- Consumes: `Oracle`, `key`, `rebuild_paragraphs`
- Produces: `is_footnote(key0, oracle) -> bool`, `mark_footnotes(paras, oracle) -> list[str]`, `drop_cover(paras, limit=60) -> tuple[list, int]` — (남은 문단, 버린 문자 수). 앞의 둘은 Task 4의 (첫 줄 키, 텍스트) 쌍 목록을 받는다

KorDocAI가 각주를 이미 페이지 끝에 순서대로 모아두므로 **위치를 옮기지 않는다.** 표시만 붙인다. 폰트 크기를 모르는 문단은 본문으로 간주한다 — 본문을 각주로 오판해 인용 블록에 가두는 쪽이 더 나쁘다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def footnotes_marked_and_cover_dropped():
    from post import mark_footnotes, drop_cover, is_footnote, Oracle, key
    kn, kb = key("1 See Nelson (1991) for a"), key("Rents are bound to the firm")
    o = Oracle(starts=set(), sizes={kn: 6.5, kb: 8.0}, body_size=8.0)
    paras = [(kb, "Rents are bound to the firm under imperfect mobility."),
             (kn, "1 See Nelson (1991) for a discussion of firm capabilities."),
             ("모르는키", "폰트를 모르는 문단은 본문으로 둔다.")]
    out = mark_footnotes(paras, o)
    assert out[0] == "Rents are bound to the firm under imperfect mobility."
    assert out[1] == "> **각주 1** See Nelson (1991) for a discussion of firm capabilities.", out[1]
    assert not out[2].startswith(">"), "모르는 문단을 각주로 오판하면 안 된다"
    assert is_footnote(kn, o) and not is_footnote(kb, o)

    kept, dropped = drop_cover([("k1", "The Cornerstones of Competitive Advantage"),
                                ("k2", "JSTOR is a not-for-profit service that helps scholars."),
                                ("k3", "All use subject to https://about.jstor.org/terms"),
                                ("k4", "본문 시작.")])
    assert [p[1] for p in kept] == ["The Cornerstones of Competitive Advantage", "본문 시작."], kept
    assert dropped > 0
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py footnotes_marked_and_cover_dropped`
Expected: FAIL — `ImportError: cannot import name 'mark_footnotes'`

- [ ] **Step 3: 구현을 추가한다**

```python
FOOTNOTE_RATIO = 0.85

COVER_PAT = re.compile(
    r"jstor|accessibility support|terms and conditions|about\.jstor\.org"
    r"|is collaborating with|not-for-profit|remediated|all use subject to", re.I)


def is_footnote(key0: str, oracle) -> bool:
    size = oracle.sizes.get(key0)
    if size is None or not oracle.body_size:     # 모르면 본문으로 둔다
        return False
    return size <= oracle.body_size * FOOTNOTE_RATIO


def mark_footnotes(paras: list, oracle) -> list[str]:
    out = []
    for k0, p in paras:
        if not is_footnote(k0, oracle):
            out.append(p)
            continue
        m = re.match(r"^(\d+)\s*(.*)$", p, re.S)
        out.append(f"> **각주 {m.group(1)}** {m.group(2)}" if m else f"> **각주** {p}")
    return out


def drop_cover(paras: list, limit: int = 60):
    """표지의 배너·약관 문단을 버린다. 서지 정보는 남는다.

    KorDocAI가 반복 바닥글을 이미 지웠으므로 남은 JSTOR 문구는 표지에만 있다.
    안전을 위해 문서 앞부분(limit개 문단)에서만 지운다.
    """
    kept, dropped = [], 0
    for i, (k0, p) in enumerate(paras):
        if i < limit and COVER_PAT.search(p):
            dropped += len(p)
        else:
            kept.append((k0, p))
    return kept, dropped
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `5/5 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/post.py scripts/selftest.py
git commit -m "$(printf '후처리: 각주 표시와 표지 배너 제거\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 6: 이미지 선별·저장과 링크 삽입

**Files:**
- Modify: `scripts/post.py`, `scripts/selftest.py`

**Interfaces:**
- Consumes: `Oracle`, `key`
- Produces: `sanitize(stem) -> str`, `pick_images(doc, skip_pages=()) -> list[tuple[int, int, object]]`, `save_images(doc, picks, img_dir, stem) -> dict[int, list[str]]` (페이지 → 마크다운 링크), `insert_images(paras, by_page, oracle) -> list[str]`, `strip_kordoc_images(md) -> str`

KorDocAI가 남긴 이미지 링크 15개는 전면 스캔 배경이므로 전부 버리고, PyMuPDF로 다시 고른다. 면적 하한 3%가 로고를, 상한 60%가 전면 스캔을 걸러낸다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def only_real_figures_are_kept():
    from post import pick_images, save_images, sanitize, strip_kordoc_images, insert_images, build_oracle
    import shutil
    doc = pymupdf.open(build_fixture())
    picks = pick_images(doc)
    assert len(picks) == 1, f"그림 1개만 남아야 한다(로고 제외): {picks}"
    assert picks[0][0] == 2, "3쪽의 그림이어야 한다"
    out = ROOT / "work" / "_fixture" / "images"
    if out.exists():
        shutil.rmtree(out)
    by_page = save_images(doc, picks, out, "fixture")
    files = list(out.glob("*"))
    assert len(files) == 1 and " " not in files[0].name, files
    assert by_page[2][0].startswith("![](images/")

    o = build_oracle(doc)
    paras = insert_images(["The final section applies the model to strategy formulation.",
                           "Implications for research are discussed at the end."], by_page, o)
    assert any(p.startswith("![](images/") for p in paras), paras
    assert strip_kordoc_images("본문\n\n![image](image_001.png)\n\n다음") == "본문\n\n다음"
    doc.close()


@test
def sanitize_removes_spaces():
    from post import sanitize
    assert sanitize("Peteraf - 2026 - The Cornerstones") == "Peteraf_-_2026_-_The_Cornerstones"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py only_real_figures_are_kept`
Expected: FAIL — `ImportError: cannot import name 'pick_images'`

- [ ] **Step 3: 구현을 추가한다**

```python
MIN_IMG_RATIO, MAX_IMG_RATIO = 0.03, 0.60


def sanitize(stem: str) -> str:
    """공백을 밑줄로 바꾼다. 경로에 공백이 있으면 이미지가 엉뚱한 폴더에 저장된다."""
    return re.sub(r"\s+", "_", stem.strip())


def strip_kordoc_images(md: str) -> str:
    """KorDocAI가 남긴 이미지 링크는 전면 스캔 배경이므로 전부 버린다."""
    keep = [u for u in md.split("\n\n") if not re.fullmatch(r"!\[[^\]]*\]\([^)]*\)", u.strip())]
    return "\n\n".join(keep)


def pick_images(doc, skip_pages=()) -> list:
    """면적 3~60%이고 한 페이지에만 나오는 이미지만 고른다.

    하한은 로고(0.62%, 1.71%)를, 상한은 전면 스캔 배경(91.32%)을 걸러낸다.
    """
    pages_of = defaultdict(set)
    cand = []
    for pno in range(doc.page_count):
        page = doc[pno]
        area = page.rect.width * page.rect.height
        for img in page.get_images(full=True):
            xref = img[0]
            for r in page.get_image_rects(xref):
                pages_of[xref].add(pno)
                cand.append((pno, xref, r, (r.width * r.height) / area))
    out = []
    for pno, xref, r, ratio in cand:
        if pno in skip_pages or len(pages_of[xref]) > 1:
            continue
        if MIN_IMG_RATIO <= ratio <= MAX_IMG_RATIO:
            out.append((pno, xref, r))
    return out


def save_images(doc, picks, img_dir, stem: str) -> dict:
    img_dir.mkdir(parents=True, exist_ok=True)
    by_page = defaultdict(list)
    for i, (pno, xref, _r) in enumerate(picks, 1):
        d = doc.extract_image(xref)
        name = f"{sanitize(stem)}-p{pno + 1:03d}-{i:02d}.{d['ext']}"
        (img_dir / name).write_bytes(d["image"])
        by_page[pno].append(f"![](images/{name})")
    return dict(by_page)


def insert_images(paras: list[str], by_page: dict, oracle) -> list[str]:
    """이미지를 해당 페이지의 마지막 문단 뒤에 넣는다."""
    if not by_page:
        return paras
    tail = {oracle.page_last.get(p): links for p, links in by_page.items() if oracle.page_last.get(p)}
    out, placed = [], set()
    for p in paras:
        out.append(p)
        for k, links in tail.items():
            if k not in placed and k in key(p):
                out.extend(links)
                placed.add(k)
    for k, links in tail.items():          # 자리를 못 찾은 이미지는 유실 대신 끝에 붙인다
        if k not in placed:
            out.extend(links)
    return out
```

파일 상단 import에 `from collections import defaultdict`를 추가한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `7/7 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/post.py scripts/selftest.py
git commit -m "$(printf '후처리: 로고·전면스캔을 뺀 이미지 선별과 링크 삽입\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 7: 후처리 전체 조립

**Files:**
- Modify: `scripts/post.py`, `scripts/selftest.py`

**Interfaces:**
- Consumes: Task 3~6의 모든 함수
- Produces: `postprocess(kordoc_md, doc, img_dir, stem) -> tuple[str, dict]` — (마크다운, 통계). 통계 키: `dropped_chars`, `figures`, `footnotes`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def postprocess_produces_clean_markdown():
    from post import postprocess
    doc = pymupdf.open(build_fixture())
    # KorDocAI 출력 형태를 모사한다 — 한 줄이 한 문단, 머리글·바닥글은 이미 제거됨
    kordoc_md = "\n\n".join([
        "## INTRODUCTION",
        "The model is developed in this", "section. Firms rely on communi-", "cation among their units.",
        "This right column follows the", "left one in reading order.",
        "Rents are bound to the firm", "under imperfect mobility.",
        "1 See Nelson (1991) for a", "discussion of firm capabilities.",
        "![image](image_001.png)",
    ])
    md, stats = postprocess(kordoc_md, doc, ROOT / "work" / "_fixture" / "images", "fixture")
    assert "communication among their units." in md, md
    assert "image_001.png" not in md, "KorDocAI 이미지 링크는 버려야 한다"
    assert "> **각주 1** See Nelson" in md, md
    assert stats["figures"] == 1 and stats["footnotes"] == 1, stats
    doc.close()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py postprocess_produces_clean_markdown`
Expected: FAIL — `ImportError: cannot import name 'postprocess'`

- [ ] **Step 3: 구현을 추가한다**

```python
def postprocess(kordoc_md: str, doc, img_dir, stem: str):
    """KorDocAI 출력을 번역 가능한 마크다운으로 다듬는다. 반환: (markdown, stats)"""
    oracle = build_oracle(doc)
    md = strip_kordoc_images(kordoc_md)
    paras = rebuild_paragraphs(md, oracle)
    paras, dropped = drop_cover(paras)

    cover = 0 if any(COVER_PAT.search(p) for p in units(kordoc_md)[:60]) else None
    picks = pick_images(doc, skip_pages=() if cover is None else (cover,))
    by_page = save_images(doc, picks, img_dir, stem) if picks else {}

    paras = mark_footnotes(paras, oracle)
    paras = insert_images(paras, by_page, oracle)
    n_notes = sum(1 for p in paras if p.startswith("> **각주"))
    return "\n\n".join(paras) + "\n", {"dropped_chars": dropped,
                                       "figures": len(picks), "footnotes": n_notes}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `8/8 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/post.py scripts/selftest.py
git commit -m "$(printf '후처리: 전체 조립 함수 postprocess 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 8: 손실 게이트

**Files:**
- Create: `scripts/quality.py`
- Modify: `scripts/selftest.py`

**Interfaces:**
- Produces: `MIN_COVERAGE = 0.97`, `MAX_TRUNCATED = 0.03`, `norm_len(t) -> int`, `truncated_ratio(md) -> float`, `score(md, raw_text, dropped_chars) -> Score`(필드 `coverage`, `truncated`, `ok`), `report(score, md) -> str`

추출과 분리해야 임계값을 단독으로 테스트하고 조정할 수 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def quality_gate_scores_correctly():
    from quality import score, norm_len
    raw = "가나다라마바사아자차카타파하" * 100                 # 1400자
    assert score(raw, raw, 0).ok
    bad = "가나다라마바사아자차카타파하" * 70                  # 30% 유실
    s = score(bad, raw, 0)
    assert not s.ok and s.coverage < 0.97, s
    assert score(bad, raw, dropped_chars=420).ok, "버린 배너는 분모에서 빼야 한다"
    trunc = ("이 문단은 마침표 없이 끝난다 " * 6).strip()
    s2 = score("\n\n".join([trunc] * 4), trunc * 4, 0)
    assert not s2.ok and s2.truncated > 0.03, s2
    assert norm_len("가 나\n다-") == 3
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py quality_gate_scores_correctly`
Expected: FAIL — `ModuleNotFoundError: No module named 'quality'`

- [ ] **Step 3: 구현을 쓴다**

```python
#!/usr/bin/env python3
"""추출 품질 채점. 기준 미달이면 번역을 시작하지 않는다.

깨진 원문을 번역하면 토큰만 쓰고 결과도 못 쓴다. 여기서 막는 것이 가장 싸다.
"""
import re
from dataclasses import dataclass

MIN_COVERAGE = 0.97   # 문자 보존율 하한
MAX_TRUNCATED = 0.03  # 잘린 문단 비율 상한

_STRIP = re.compile(r"[\s­\-]")
_ENDINGS = tuple(".!?\"')]”’」』")


@dataclass
class Score:
    coverage: float
    truncated: float
    ok: bool


def norm_len(t: str) -> int:
    """공백·하이픈을 뺀 문자 수. 줄바꿈 처리 차이가 점수에 영향을 주지 않게 한다."""
    return len(_STRIP.sub("", t))


def truncated_ratio(md: str) -> float:
    paras = [p.strip() for p in md.split("\n\n")
             if len(p.strip()) > 80 and not p.lstrip().startswith(("#", "|", ">", "!"))]
    if not paras:
        return 0.0
    return len([p for p in paras if not p.endswith(_ENDINGS)]) / len(paras)


def score(md: str, raw_text: str, dropped_chars: int) -> Score:
    denom = norm_len(raw_text) - max(dropped_chars, 0)
    cov = norm_len(md) / denom if denom > 0 else 0.0
    tr = truncated_ratio(md)
    return Score(cov, tr, cov >= MIN_COVERAGE and tr <= MAX_TRUNCATED)


def report(s: Score, md: str) -> str:
    lines = ["# 추출 품질 리포트", "",
             f"- 문자 보존율: {s.coverage:.3f} (기준 {MIN_COVERAGE})",
             f"- 잘린 문단 비율: {s.truncated:.3f} (기준 {MAX_TRUNCATED})",
             f"- 판정: {'통과' if s.ok else '미달 — 번역을 시작하지 않음'}", ""]
    bad = [p.strip()[:100] for p in md.split("\n\n")
           if len(p.strip()) > 80 and not p.lstrip().startswith(("#", "|", ">", "!"))
           and not p.strip().endswith(_ENDINGS)]
    if bad:
        lines += ["## 잘린 것으로 의심되는 문단", ""] + [f"- …{b}" for b in bad[:20]]
    return "\n".join(lines) + "\n"
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `9/9 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/quality.py scripts/selftest.py
git commit -m "$(printf '검증: 추출 손실 게이트(보존율·잘린 문단) 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 9: extract.py 통합, 폴백, 인코딩 수정

**Files:**
- Modify: `scripts/extract.py` (`extract_pdf` 교체, `main` 일부), `scripts/chunk.py:70`, `scripts/selftest.py`

**Interfaces:**
- Consumes: `kordoc.to_markdown`, `post.postprocess`, `quality.score`, `quality.report`
- Produces: `extract.py`가 게이트 미달 시 `work/<stem>/extract_report.md`를 남기고 exit 1

기존 pymupdf4llm 경로는 지우지 않고 **폴백**으로 남긴다. KorDocAI CLI가 없거나(오프라인) 점수가 낮으면 그쪽으로 재시도한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def extract_runs_and_writes_utf8():
    import json, shutil, subprocess
    out = ROOT / "work" / "_fixture_run"
    if out.exists():
        shutil.rmtree(out)
    r = subprocess.run([sys.executable, "scripts/extract.py", str(build_fixture()), "--out", str(out)],
                       cwd=ROOT, capture_output=True, text=True, encoding="utf-8")
    assert r.returncode == 0, r.stdout + r.stderr
    md = (out / "source.md").read_text(encoding="utf-8")
    assert "communication among their units." in md, md[:400]
    assert "This content downloaded" not in md
    meta = json.loads((out / "meta.json").read_text(encoding="utf-8"))  # cp949면 여기서 깨진다
    assert meta["figures"] == 1 and meta["coverage"] >= 0.97, meta
    assert meta["extractor"] in ("kordoc", "pymupdf4llm"), meta
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py extract_runs_and_writes_utf8`
Expected: FAIL — `meta.json`에 `coverage` 키가 없다(KeyError)

- [ ] **Step 3: `extract.py`의 PDF 경로를 교체한다**

기존 `extract_pdf()`의 이름을 `extract_pdf_legacy()`로 바꾸고(본문은 그대로 둔다), 아래를 추가한다.

```python
def extract_pdf(src: Path, outdir: Path):
    """KorDocAI CLI + 후처리. 반환: (markdown, stats, raw_text) — 실패 시 md는 None."""
    import pymupdf
    import kordoc, post

    raw_md = kordoc.to_markdown(src)
    doc = pymupdf.open(str(src))
    raw_text = "".join(doc[i].get_text() for i in range(doc.page_count))
    if raw_md is None:
        doc.close()
        return None, {}, raw_text
    md, stats = post.postprocess(raw_md, doc, outdir / "images", src.stem)
    doc.close()
    stats["extractor"] = "kordoc"
    return md, stats, raw_text
```

`main()`의 PDF 분기를 이렇게 바꾼다.

```python
    if ext == ".pdf":
        md, stats, raw_text = extract_pdf(src, outdir)
        s = None
        if md:
            md = clean(md)
            s = quality.score(md, raw_text, stats.get("dropped_chars", 0))
        if s is None or not s.ok:              # 폴백 — 기존 pymupdf4llm 경로
            md2 = clean(extract_pdf_legacy(src, outdir))
            s2 = quality.score(md2, raw_text, 0)
            if s is None or s2.coverage > s.coverage:
                md, s = md2, s2
                stats = {**stats, "extractor": "pymupdf4llm",
                         "figures": len(list((outdir / "images").glob("*")))}
        if not s.ok:
            (outdir / "extract_report.md").write_text(quality.report(s, md), encoding="utf-8")
            (outdir / "source.md").write_text(md, encoding="utf-8")
            sys.exit(f"추출 품질 미달 — 보존율 {s.coverage:.3f}, 잘린 문단 {s.truncated:.3f}. "
                     f"번역을 시작하지 않는다. 리포트: {outdir / 'extract_report.md'}")
    elif ext == ".docx":
        md = clean(extract_docx(src, outdir))
        s = quality.Score(1.0, quality.truncated_ratio(md), True)
        stats = {"extractor": "mammoth", "figures": len(list((outdir / "images").glob("*")))}
    else:
        sys.exit("supported: .pdf .docx")

    (outdir / "source.md").write_text(md, encoding="utf-8")
    meta = {"source": str(src), "type": ext[1:], "chars": len(md),
            "figures": stats.get("figures", 0), "footnotes": stats.get("footnotes", 0),
            "extractor": stats.get("extractor", "unknown"),
            "coverage": round(s.coverage, 4), "truncated": round(s.truncated, 4)}
    (outdir / "meta.json").write_text(json.dumps(meta, indent=2, ensure_ascii=False),
                                      encoding="utf-8")
    print(json.dumps(meta, ensure_ascii=False))
    print(f"→ {outdir / 'source.md'}")
```

파일 상단에 `import quality`를 추가하고, 그 위에 `sys.path.insert(0, str(Path(__file__).resolve().parent))`를 둔다.

- [ ] **Step 4: `chunk.py:70`의 인코딩을 고친다**

```python
    (cdir / "index.json").write_text(json.dumps(index, indent=2, ensure_ascii=False),
                                     encoding="utf-8")
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `10/10 통과`

- [ ] **Step 6: 커밋**

```bash
git add scripts/extract.py scripts/chunk.py scripts/selftest.py
git commit -m "$(printf '추출: KorDocAI 경로 적용, pymupdf4llm 폴백, JSON utf-8 인코딩\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 10: verify.py 검사 4종 추가

**Files:**
- Modify: `scripts/verify.py`, `scripts/selftest.py`

**Interfaces:**
- Produces: `check_images(src, tr, wd) -> list[str]`, `check_numbers(src, tr) -> tuple[list[str], list[str]]`, `check_citations(src, tr) -> list[str]`, `check_paragraphs(src, tr) -> list[str]`

이미지 링크가 문자열로만 확인되던 구멍이 지난 유실을 통과시켰다. 파일 실존을 본다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
@test
def verify_checks_are_strict():
    from verify import check_images, check_numbers, check_citations, check_paragraphs
    wd = ROOT / "work" / "_fixture_run"
    src = "![](images/nope.png)\n\nIn 1993 the value was 3.14 (Peteraf, 1993).\n\n둘째 문단."
    tr = "![](images/nope.png)\n\n1993년 값은 3.14였다 (Peteraf, 1993).\n\n둘째 문단."
    assert check_images(src, tr, wd), "없는 파일은 잡혀야 한다"
    assert check_numbers(src, tr)[0] == []
    assert check_citations(src, tr) == []
    assert check_paragraphs(src, tr) == []
    bad = "값은 였다 (Peteraf, 1991).\n\n둘째 문단."
    assert check_numbers(src, bad)[0], "빠진 연도·소수는 FAIL이어야 한다"
    assert check_citations(src, bad), "인용 변형은 잡혀야 한다"
    assert check_paragraphs(src, bad), "문단 수 불일치는 잡혀야 한다"
    counted = "네 가지 조건이 있다."
    assert check_numbers("There are 4 conditions.", counted)[0] == [], "정수는 WARN까지만"
    assert check_numbers("There are 4 conditions.", counted)[1], "정수 누락은 WARN으로 보고"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python scripts/selftest.py verify_checks_are_strict`
Expected: FAIL — `ImportError: cannot import name 'check_images'`

- [ ] **Step 3: `verify.py`에 검사를 추가한다**

`main()` 위에 넣는다.

```python
CITE = re.compile(r"\(([A-Z][A-Za-z&.\- ]+?),\s*(\d{4}[a-z]?)\)")
YEAR_OR_DECIMAL = re.compile(r"\b\d{4}\b|\b\d+\.\d+\b")
INTEGER = re.compile(r"\b\d+\b")


def check_images(src: str, tr: str, wd) -> list[str]:
    """링크 문자열이 아니라 파일이 실제로 있는지 본다."""
    probs = []
    for img in IMG.findall(src):
        if img not in tr:
            probs.append(f"missing image link {img}")
        elif not (Path(wd) / img).exists():
            probs.append(f"image file not found: {img}")
    return probs


def check_numbers(src: str, tr: str):
    """연도·소수는 FAIL, 그 밖의 정수는 WARN. 'four → 네 가지'처럼 한글 수사가 되는 경우가 있다."""
    fail = [f"missing number {n}" for n in
            sorted(set(YEAR_OR_DECIMAL.findall(src)) - set(YEAR_OR_DECIMAL.findall(tr)))]
    src_i = set(INTEGER.findall(src)) - set(YEAR_OR_DECIMAL.findall(src))
    warn = [f"number not found (확인 필요): {n}" for n in sorted(src_i - set(INTEGER.findall(tr)))]
    return fail, warn


def check_citations(src: str, tr: str) -> list[str]:
    missing = set(CITE.findall(src)) - set(CITE.findall(tr))
    return [f"missing citation ({a}, {y})" for a, y in sorted(missing)]


def check_paragraphs(src: str, tr: str) -> list[str]:
    ns = len([p for p in src.split("\n\n") if p.strip()])
    nt = len([p for p in tr.split("\n\n") if p.strip()])
    return [f"paragraph count {ns}→{nt}"] if ns != nt else []
```

기존 이미지 문자열 검사 블록(`for img in IMG.findall(src): …`)을 지우고 루프 안에 아래를 넣는다.

```python
        probs += check_images(src, tr, wd)
        nfail, nwarn = check_numbers(src, tr)
        probs += nfail + check_citations(src, tr) + check_paragraphs(src, tr)
        for w in nwarn:
            print(f"WARN {e['id']}  {w}")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python scripts/selftest.py`
Expected: `11/11 통과`

- [ ] **Step 5: 커밋**

```bash
git add scripts/verify.py scripts/selftest.py
git commit -m "$(printf '검증: 이미지 실존·숫자·인용·문단 수 검사 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 11: 전문 통독 에이전트

**Files:**
- Create: `.claude/agents/final-reviewer.md`
- Modify: `.claude/commands/translate.md`, `.claude/skills/academic-korean/SKILL.md`

**Interfaces:**
- Consumes: `output/<stem>/<stem>.ko.md`, `work/<stem>/glossary.md`, `work/<stem>/chunks/*.md`
- Produces: `work/<stem>/final_review.md`, 수정된 `work/<stem>/translated/NNN.md`

청크 단위 번역·검수는 아무도 문서 전체를 읽지 않는다. **수정은 조립본이 아니라 청크 파일에 적용한다** — 청크가 단일 원천이어야 `verify.py`가 계속 유효하다.

- [ ] **Step 1: 에이전트를 작성한다**

`.claude/agents/final-reviewer.md`:

```markdown
---
name: final-reviewer
description: 조립된 번역본 전문을 통독하여 문맥·용어 일관성·역어 적절성을 점검하고 청크 파일을 수정한다. 파이프라인 마지막 검토 단계.
tools: Read, Edit, Write, Grep
model: opus
skills: academic-korean
---

번역본 **전문**을 통독하여 청크 단위 작업에서는 보이지 않는 문제를 잡는다.

## 읽기
1. `output/<stem>/<stem>.ko.md` 전문을 읽는다.
2. `work/<stem>/glossary.md`를 읽는다.
3. 의심되는 대목만 해당 `work/<stem>/chunks/NNN.md`를 열어 원문과 대조한다. 원문 전체를 읽지 않는다.

## 점검 항목
- **청크 경계 흐름**: 지시어("이 결과는", "앞서 논의한")가 가리키는 대상이 실제로 앞에 있는가.
- **용어 흔들림**: 같은 개념이 앞뒤로 다르게 번역됐는가.
- **역어 적절성**: glossary 역어가 그 문장 맥락에서 실제로 맞는가.
- **어색한 문단**: 단어 교체로 안 되면 문단째 다시 쓴다.

## 수정
- **수정은 `work/<stem>/translated/NNN.md`에 Edit으로 적용한다.** 조립본(`ko.md`)을 직접 고치지 않는다.
- 이미지 링크·표 구조·헤딩 수·문단 수를 바꾸지 않는다.
- 원문에 없는 내용을 추가하지 않는다. 근거가 원문에 없으면 고치지 말고 확인 목록에 올린다.

## 분류
| 분류 | 처리 |
|---|---|
| 명확 — 용어 흔들림 통일, 지시어 연결, 번역투 | 직접 수정하고 변경 표에 기록 |
| 판단 필요 — 역어 선택이 갈리는 용어, 원문 자체가 모호한 대목 | **수정하지 말고** 확인 요청 목록에 올린다 |

## 산출물 `work/<stem>/final_review.md`

    # 전문 통독 보고

    ## 자동 반영한 수정 (N건)
    | # | 청크 | 유형 | 수정 전 | 수정 후 | 근거 |

    ## 사용자 확인 요청 (N건)
    | # | 위치 | 쟁점 | 현재 역어 | 대안 | 판단 근거 |

수정이 없으면 "수정 없음"이라고 쓴다. 없는 문제를 만들어내지 않는다.
```

- [ ] **Step 2: `translate.md`의 6~8절을 교체한다**

```markdown
## 6. 조립
```bash
python scripts/assemble.py work/STEM
```

## 7. 전문 통독 — `final-reviewer` 서브에이전트 1회
프롬프트: "output/STEM/STEM.ko.md 전문을 통독하고, 수정은 work/STEM/translated/NNN.md에 적용한 뒤 work/STEM/final_review.md에 보고하라."

## 8. 재검증·재조립
```bash
python scripts/verify.py work/STEM
python scripts/assemble.py work/STEM
python scripts/to_html.py output/STEM
```

## 9. 최종 보고
- 출력 경로: `output/STEM/STEM.ko.md`, `output/STEM/STEM.html`
- `meta.json`의 추출기·보존율·잘린 문단 비율, 청크 수, 검수 수정 건수, 통독 수정 건수
- `final_review.md`의 **사용자 확인 요청 목록을 그대로 옮겨 적는다**
- 용어 변경을 원하면 glossary 갱신 후 일괄 교체·재조립이 가능함을 안내한다
```

1절 끝에 한 줄 추가한다: `extract.py`가 0이 아닌 코드로 끝나면 **번역 단계로 넘어가지 않는다.** `work/STEM/extract_report.md`를 읽어 어느 문단이 잘렸는지 사용자에게 보고하고 멈춘다.

- [ ] **Step 3: `academic-korean/SKILL.md`에 각주 규칙을 넣는다**

```markdown
## 각주

- `> **각주 N** …` 형태의 인용 블록은 각주다. 본문과 같은 서술체로 번역한다.
- `> **각주 N**` 표시 자체는 그대로 둔다. 각주를 본문 문단으로 끌어올리지 않는다.
- 각주 안의 서지 정보(저자·연도·서명)는 본문 참고문헌과 같은 규칙으로 원문을 유지한다.
```

- [ ] **Step 4: 문서 정합성을 확인한다**

Run: `python -c "import pathlib; t=pathlib.Path('.claude/commands/translate.md').read_text(encoding='utf-8'); assert 'final-reviewer' in t and 'extract_report.md' in t; a=pathlib.Path('.claude/agents/final-reviewer.md').read_text(encoding='utf-8'); assert 'translated/NNN.md' in a; print('ok')"`
Expected: `ok`

- [ ] **Step 5: 커밋**

```bash
git add .claude/agents/final-reviewer.md .claude/commands/translate.md .claude/skills/academic-korean/SKILL.md
git commit -m "$(printf '검수: 전문 통독 에이전트와 파이프라인 단계 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 12: 명령서와 CLAUDE.md

**Files:**
- Create: `명령서.md`, `CLAUDE.md`
- Modify: `PLAN.md`

규칙이 `translate.md`·`SKILL.md`·에이전트 정의 세 곳에 흩어져 있어 어디를 고쳐야 할지 알 수 없다. 한 곳에 모으고, 사용자가 여기만 고치면 되게 한다.

- [ ] **Step 1: `명령서.md`를 작성한다**

```markdown
# 논문 번역기 명령서

이 파일이 번역기의 규칙이다. 마음에 안 드는 동작이 있으면 **여기를 고치면 된다.**
직접 고치기 애매하면 맨 아래 "불편사항 기록"에 한 줄 적어두고 Claude에게 이 파일과 함께 보여주면 된다.

## 무엇을 하는 도구인가
영어 논문(PDF·DOCX)을 한국어로 **완역**한다. 요약하지 않는다. 그림은 번역하지 않고 그대로 둔다.

## 절대 규칙
- 원문 충실도가 최우선이다. 속도·비용보다 앞선다.
- 원문에 없는 내용을 추론해 채우지 않는다. 원문이 모호하면 확인 목록에 올린다.
- 수치·인용·표는 원문 그대로 유지한다.
- 문체는 학술 서술체(~다/~이다). 개조식으로 쓰지 않는다.

## 파이프라인
| 단계 | 담당 | 하는 일 |
|---|---|---|
| 1 | `extract.py` → KorDocAI CLI + `post.py` | PDF → `source.md`. 품질 미달이면 **여기서 중단** |
| 2 | `chunk.py` | 헤딩 기준 분할 |
| 3 | `glossary-builder` | 용어 대응표 작성 |
| 4 | `translator` ×N | 청크별 완역(병렬) |
| 5 | `verify.py` | 기계 검증 — 이미지 실존·숫자·인용·문단 수·한글 비율 |
| 6 | `reviewer` ×N | 청크별 원문 대조 검수 |
| 7 | `assemble.py` | 병합 + 용어표 부록 |
| 8 | `final-reviewer` | **전문 통독** — 문맥·용어 일관성·역어 적절성 |
| 9 | `verify.py` → `assemble.py` → `to_html.py` | 재검증·재조립·HTML |

## 조정할 수 있는 값

바꾸려면 아래 표의 파일에서 같은 이름의 상수를 고친다. 값을 바꾸면 이 표도 함께 고친다.

| 항목 | 값 | 위치 |
|---|---|---|
| 문자 보존율 하한 | 0.97 | `scripts/quality.py: MIN_COVERAGE` |
| 잘린 문단 비율 상한 | 0.03 | `scripts/quality.py: MAX_TRUNCATED` |
| 각주 폰트 판정 | 본문 중앙값의 0.85배 이하 | `scripts/post.py: FOOTNOTE_RATIO` |
| 이미지 채택 면적 | 3%~60% | `scripts/post.py: MIN_IMG_RATIO, MAX_IMG_RATIO` |
| 청크 크기 | 6,000자 | `scripts/chunk.py: --max` |
| 추출기 옵션 | `--no-tables` (필수) | `scripts/kordoc.py: to_markdown` |

## 처리 방침
- **각주**: `> **각주 N**` 형태로 표시하고 번역한다. KorDocAI가 페이지 끝에 모아둔 위치를 그대로 둔다.
- **머리말·푸터·JSTOR 배너**: 삭제한다. 머리말·푸터는 KorDocAI가, 표지 약관은 `post.py`가 지운다.
- **표지**: 서지 정보만 남기고 이용약관 문단은 버린다.
- **그림**: 로고(면적 3% 미만)와 전면 스캔 배경(60% 초과)은 제외한다. 남은 그림은 HTML에 base64로 넣는다.
- **참고문헌**: 번역하지 않는다.
- **역어**: 하나만 쓴다. 원어 병기는 첫 등장 한 번만, 괄호 한 쌍만.

## 통독 단계에서 자동으로 고치는 것과 물어보는 것
| 자동 수정 | 사용자 확인 |
|---|---|
| 같은 용어가 앞뒤로 다르게 번역된 것 | 역어 선택이 갈리는 용어 |
| 청크 경계의 지시어 연결 | 원문 자체가 모호한 대목 |
| 번역투·어색한 문장 | 원문에 근거가 없어 판단이 필요한 곳 |

## 쓰는 법
```
논문번역기.bat 더블클릭  →  브라우저에서 PDF를 끌어다 놓는다
```
진행 상황이 막대로 표시되고, 끝나면 "번역본 보기"로 HTML을 연다.

## 결과를 받은 뒤 고치고 싶을 때
Claude에게 이렇게 말하면 된다. **번역을 처음부터 다시 하지 않는다.**
- "`fungible resource`를 `전용 가능 자원`으로 바꿔줘" → 용어표 갱신 후 전 문서 일괄 교체·재조립
- "3장 문체가 어색해" → 해당 청크만 다시 검수
- "각주를 문서 맨 끝으로 몰아줘" → 이 명령서의 처리 방침을 고치고 재조립

## 추출이 실패하면
`work/<제목>/extract_report.md`에 어느 문단이 잘렸는지 적혀 있다. 웹 UI의 "로그 보기"로도 볼 수 있다.
번역을 시작하지 않고 멈추는 것이 정상 동작이다 — 깨진 원문을 번역해봐야 쓸 수 없다.

## 아직 안 켠 기능
필요해지면 말만 하면 된다. KorDocAI 옵션으로 바로 켤 수 있다.
- 수식이 많은 논문 → `--formula-ocr`
- 스캔본(텍스트 없는 PDF) → `--ocr`

## 불편사항 기록

쓰다가 걸린 점을 아래에 한 줄씩 적어둔다. 다음 개선 작업의 입력이 된다.

- (예시) 표가 있는 논문에서 표 안 문장이 잘렸다 — 2026-09-04
-
```

- [ ] **Step 2: `CLAUDE.md`를 작성한다**

```markdown
# paper-translator

이 폴더의 모든 작업은 **`명령서.md`를 따른다.** 작업 전에 반드시 읽는다.

- 규칙·임계값·처리 방침의 단일 출처는 `명령서.md`다. 코드와 명령서가 어긋나면 사용자에게 알리고 함께 고친다.
- 원문 충실도가 최우선이다. 원문에 없는 내용을 만들지 않는다.
- 파일 이동·병합·검증은 스크립트가, 문장 작성은 서브에이전트가 한다. 이 경계를 넘지 않는다.
- 설계 문서: `docs/superpowers/specs/`, 구현 계획: `docs/superpowers/plans/`
```

- [ ] **Step 3: `PLAN.md`를 갱신한다**

4-1절의 도구 선정 표에 KorDocAI 행을 넣고 채택 근거(보존율 0.972 대 0.686)를 적는다. 5절 2단계 목록에서 완료 항목에 `[x]`를 단다: 실제 논문 벤치마크, verify.py 강화, 컨텍스트 이월(전문 통독으로 대체). 남은 항목(humanizer 스킬, 모델 A/B, glossary 확인 게이트)은 그대로 둔다.

- [ ] **Step 4: 명령서와 코드의 값이 일치하는지 확인한다**

Run: `python -c "import pathlib; q=pathlib.Path('scripts/quality.py').read_text(encoding='utf-8'); p=pathlib.Path('scripts/post.py').read_text(encoding='utf-8'); m=pathlib.Path('명령서.md').read_text(encoding='utf-8'); assert 'MIN_COVERAGE = 0.97' in q and '| 0.97 |' in m; assert 'MAX_TRUNCATED = 0.03' in q and '| 0.03 |' in m; assert 'FOOTNOTE_RATIO = 0.85' in p; print('ok')"`
Expected: `ok`

- [ ] **Step 5: 커밋**

```bash
git add 명령서.md CLAUDE.md PLAN.md
git commit -m "$(printf '문서: 사용자가 고치는 명령서와 CLAUDE.md 추가\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```

---

### Task 13: 실제 논문 회귀 검증

**Files:** 없음(측정과 확인만). 문제가 나오면 해당 태스크로 돌아간다.

합성 픽스처가 통과해도 실제 논문에서 깨질 수 있다. 두 논문으로 확인한다.

- [ ] **Step 1: 전체 자체 테스트를 돌린다**

Run: `python scripts/selftest.py`
Expected: `11/11 통과`

- [ ] **Step 2: Peteraf 추출을 다시 돌려 수치를 잰다**

```bash
python scripts/extract.py "input/Peteraf - 2026 - The Cornerstones of Competitive Advantage A Resource-Based View.pdf"
```
Expected: exit 0, `extractor` = `kordoc`, `coverage` ≥ 0.97, `truncated` ≤ 0.03, `figures` = 0(로고·전면스캔 제외)

기준 미달이면 `work/<stem>/extract_report.md`의 잘린 문단 목록을 보고 Task 4~7 중 해당 규칙을 고친다.

- [ ] **Step 3: 눈으로 대조한다**

```bash
python -c "import pathlib; print(pathlib.Path('work/Peteraf - 2026 - The Cornerstones of Competitive Advantage A Resource-Based View/source.md').read_text(encoding='utf-8')[:3000])"
```
확인 사항 — 아래가 모두 만족돼야 한다.
- `This content downloaded` 0회, `M. A. Peteraf` 러닝헤드 0회
- 초록이 `…applications of the model for`에서 끊기지 않고 이어진다
- `ous strands of research`처럼 단어가 반토막 난 곳이 없다
- 한 문단이 여러 문장으로 이어져 있다(한 줄이 한 문단이 아니다)
- 각주가 `> **각주 N**`으로 표시돼 있다

- [ ] **Step 4: sample.pdf 회귀를 확인한다**

```bash
python scripts/extract.py input/sample.pdf && python scripts/chunk.py work/sample
python -c "import json,pathlib; m=json.loads(pathlib.Path('work/sample/meta.json').read_text(encoding='utf-8')); print(m); assert m['figures']==2, '기존 그림 2개가 유지돼야 한다'"
```
Expected: `figures` = 2, 표 1개가 `source.md`에 `|` 행으로 남아 있음

`--no-tables`로 표 감지를 껐으므로 sample.pdf의 표가 깨질 수 있다. 깨지면 `kordoc.py`에서 표가 있는 문서에 한해 `--no-tables`를 빼는 대신, 표 유무를 PyMuPDF로 먼저 판정해 옵션을 나누는 방식으로 고친다.

- [ ] **Step 5: 전체 파이프라인을 한 번 돌린다**

`논문번역기.bat`을 띄우고 Peteraf PDF를 올린다. 또는 `claude` 세션에서 `/translate "input/Peteraf - 2026 - The Cornerstones of Competitive Advantage A Resource-Based View.pdf"`.

확인 사항:
- `work/<stem>/final_review.md`가 생성되고 확인 요청 목록이 최종 보고에 그대로 나온다
- `output/<stem>/<stem>.html`에 깨진 `<img>`가 없다
- 각주가 절 끝에 번역돼 있다

- [ ] **Step 6: 결과를 기록하고 커밋한다**

`PLAN.md`에 "2026-09-04 재검증" 항목을 만들어 before/after 수치를 적는다: 보존율 0.686 → 실측값, 잘린 문단 비율, JSTOR 푸터 14회 → 0회.

```bash
git add PLAN.md
git commit -m "$(printf '검증: Peteraf·sample 회귀 결과 기록\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>')"
```
