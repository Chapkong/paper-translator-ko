# 논문 번역기 명령서

이 파일이 번역기의 규칙이다. 마음에 안 드는 동작이 있으면 **여기를 고치면 된다.**
직접 고치기 애매하면 맨 아래 "불편사항 기록"에 한 줄 적어두고 Claude에게 이 파일과 함께 보여주면 된다.

## 무엇을 하는 도구인가

영어 논문(PDF·DOCX)을 한국어로 **완역**한다. 요약하지 않는다. 그림은 번역하지 않고 그대로 둔다.

## 절대 규칙

- 원문 충실도가 최우선이다. 속도·비용보다 앞선다.
- 원문에 없는 내용을 추론해 채우지 않는다. 원문이 모호하면 확인 목록에 올린다.
- 수치·인용·표는 원문 그대로 유지한다.
- 문체는 학술 서술체(~다/~이다). 개조식으로 쓰지 않는다.

## 파이프라인

| 단계 | 담당 | 하는 일 |
|---|---|---|
| 1 | `extract.py` → KorDocAI CLI + `post.py` | PDF → `source.md`. 품질 미달이면 **여기서 중단** |
| 2 | `chunk.py` | 헤딩 기준 분할 |
| 3 | `glossary-builder` | 용어 대응표 작성 |
| 4 | `translator` ×N | 청크별 완역(병렬) |
| 5 | `verify.py` | 기계 검증 — 이미지 실존·숫자·인용·문단 수·한글 비율 |
| 6 | `reviewer` ×N | 청크별 원문 대조 검수 |
| 7 | `assemble.py` | 병합 + 용어표 부록 |
| 8 | `final-reviewer` | **전문 통독** — 문맥·용어 일관성·역어 적절성 |
| 9 | `verify.py` → `assemble.py` → `to_html.py` | 재검증·재조립·HTML |
| 10 | `output-verifier` | **결과물 판정** — PASS/FAIL. FAIL이면 최대 2회 재수정 |

검사가 네 겹이다. 앞의 셋(`verify.py`·`reviewer`·`final-reviewer`)은 **고치는** 역할이고,
마지막 `output-verifier`는 **합격을 결정하는** 역할이라 고칠 권한이 없다(도구가 읽기 전용).

## 조정할 수 있는 값

바꾸려면 아래 표의 파일에서 같은 이름의 상수를 고친다. 값을 바꾸면 이 표도 함께 고친다.

| 항목 | 값 | 위치 |
|---|---|---|
| 문자 보존율 하한 | 0.97 | `scripts/quality.py: MIN_COVERAGE` |
| 잘린 문단 비율 상한 | 0.03 | `scripts/quality.py: MAX_TRUNCATED` |
| 각주 폰트 판정 | 본문 중앙값의 0.85배 이하 | `scripts/post.py: FOOTNOTE_RATIO` |
| 이미지 채택 면적 | 3%~60% | `scripts/post.py: MIN_IMG_RATIO, MAX_IMG_RATIO` |
| 반복 요소 판정 | 3쪽 이상에 등장 | `scripts/post.py: repeated_chars(min_pages=)` |
| 청크 크기 | 6,000자 | `scripts/chunk.py: --max` |
| 추출기 옵션 | `--no-tables` (필수) | `scripts/kordoc.py: to_markdown` |

`--no-tables`는 빼면 안 된다. 기본값에서는 2단 조판의 테두리를 표로 오인해 좌우 단이 한 줄에 섞인다.

## 처리 방침

- **각주**: `> **각주 N**` 형태로 표시하고 번역한다. KorDocAI가 페이지 끝에 모아둔 위치를 그대로 둔다.
- **머리말·푸터·JSTOR 배너**: 삭제한다. 머리말·푸터는 KorDocAI가, 표지 약관은 `post.py`가 지운다.
- **표지**: 서지 정보만 남기고 이용약관 문단은 버린다.
- **그림**: 로고(면적 3% 미만)와 전면 스캔 배경(60% 초과)은 제외한다. 남은 그림은 HTML에 base64로 넣는다.
- **참고문헌**: 번역하지 않는다.
- **역어**: 하나만 쓴다. 원어 병기는 첫 등장 한 번만, 괄호 한 쌍만.

## 통독 단계에서 자동으로 고치는 것과 물어보는 것

| 자동 수정 | 사용자 확인 |
|---|---|
| 같은 용어가 앞뒤로 다르게 번역된 것 | 역어 선택이 갈리는 용어 |
| 청크 경계의 지시어 연결 | 원문 자체가 모호한 대목 |
| 번역투·어색한 문장 | 원문에 근거가 없어 판단이 필요한 곳 |

## 쓰는 법

```
논문번역기.bat 더블클릭  →  브라우저에서 PDF를 끌어다 놓는다
```

진행 상황이 막대로 표시되고, 끝나면 "번역본 보기"로 HTML을 연다.

## 결과를 받은 뒤 고치고 싶을 때

Claude에게 이렇게 말하면 된다. **번역을 처음부터 다시 하지 않는다.**

- "`fungible resource`를 `전용 가능 자원`으로 바꿔줘" → 용어표 갱신 후 전 문서 일괄 교체·재조립
- "3장 문체가 어색해" → 해당 청크만 다시 검수
- "각주를 문서 맨 끝으로 몰아줘" → 이 명령서의 처리 방침을 고치고 재조립

## 추출이 실패하면

`work/<제목>/extract_report.md`에 어느 문단이 잘렸는지 적혀 있다. 웹 UI의 "로그 보기"로도 볼 수 있다.
번역을 시작하지 않고 멈추는 것이 정상 동작이다 — 깨진 원문을 번역해봐야 쓸 수 없다.

KorDocAI CLI가 없거나(오프라인) 결과가 나쁘면 기존 pymupdf4llm 경로로 한 번 더 시도한 뒤,
그래도 미달이면 멈춘다. `meta.json`의 `extractor` 항목에 어느 경로가 쓰였는지 적힌다.

## 아직 안 켠 기능

필요해지면 말만 하면 된다. KorDocAI 옵션으로 바로 켤 수 있다.

- 수식이 많은 논문 → `--formula-ocr`
- 스캔본(텍스트 없는 PDF) → `--ocr`

## 불편사항 기록

쓰다가 걸린 점을 아래에 한 줄씩 적어둔다. 다음 개선 작업의 입력이 된다.

- (예시) 표가 있는 논문에서 표 안 문장이 잘렸다 — 2026-09-04
-
