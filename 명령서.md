# 논문 번역기 명령서

이 파일이 번역기의 규칙이다. 마음에 안 드는 동작이 있으면 **여기를 고치면 된다.**
직접 고치기 애매하면 맨 아래 "불편사항 기록"에 한 줄 적어두고 Claude에게 이 파일과 함께 보여주면 된다.

## 무엇을 하는 도구인가

영어 논문(PDF·DOCX)을 한국어로 **완역**한다. 요약하지 않는다. 그림은 번역하지 않고 그대로 둔다.

## 절대 규칙

- 원문 충실도가 최우선이다. 속도·비용보다 앞선다.
- 원문에 없는 내용을 추론해 채우지 않는다. 원문이 모호하면 확인 목록에 올린다.
- 수치·인용·표는 원문 그대로 유지한다.
- 문체는 학술 서술체(~다/~이다). 개조식으로 쓰지 않는다.

## 파이프라인

| 단계 | 담당 | 하는 일 |
|---|---|---|
| 1 | `extract.py` → 후보 3종 채점 + `post.py` | PDF → `source.md`. 품질 미달이면 **여기서 중단** |
| 2 | `chunk.py` | 헤딩 기준 분할 |
| 3 | `glossary-builder` | 용어 대응표 작성 |
| 4 | `translator` ×N | 청크별 완역(병렬) |
| 5 | `verify.py` | 기계 검증 — 이미지 실존·숫자·인용·문단 수·한글 비율 |
| 6 | `reviewer` ×N | 청크별 원문 대조 검수 |
| 7 | `assemble.py` | 병합 + 용어표 부록 |
| 8 | `final-reviewer` | **전문 통독** — 문맥·용어 일관성·역어 적절성 |
| 9 | `verify.py` → `assemble.py` → `to_html.py` | 재검증·재조립·HTML |
| 10 | `final_check.py` | **기계 최종 검증** — 미번역 각주·문단, 문단 수, 빠진 연도, 이미지 실존을 직접 센다 |
| 11 | `output-verifier` | **결과물 판정** — PASS/FAIL. 둘 다 통과해야 하고, 어긋나면 기계 결과를 따른다 |

검사가 다섯 겹이다. 앞의 셋(`verify.py`·`reviewer`·`final-reviewer`)은 **고치는** 역할이고,
`output-verifier`는 **합격을 결정하는** 역할이라 고칠 권한이 없다(도구가 읽기 전용).

그 위에 `final_check.py`가 있다. **에이전트의 PASS 선언을 믿지 않고 결과물을 직접 센다.**
첫 실행은 "ALL PASS"를 보고했지만 원문의 3분의 1이 없었고, 두 번째 실행은 각주 25개 중
17개가 영어로 남았는데도 PASS가 나왔다. 판정하는 주체와 판정받는 주체가 같으면 반복된다.
`work/<제목>/final_check.md`에 수치 표가 남고, 최종 보고에 그 표가 그대로 실린다.

## 조정할 수 있는 값

바꾸려면 아래 표의 파일에서 같은 이름의 상수를 고친다. 값을 바꾸면 이 표도 함께 고친다.

| 항목 | 값 | 위치 |
|---|---|---|
| 문자 보존율 하한 | 0.97 | `scripts/quality.py: MIN_COVERAGE` |
| 잘린 문단 비율 상한 | 0.05 | `scripts/quality.py: MAX_TRUNCATED` |
| 각주 폰트 판정 | 본문 중앙값의 0.85배 이하 | `scripts/post.py: FOOTNOTE_RATIO` |
| 이미지 채택 면적 | 3%~60% | `scripts/post.py: MIN_IMG_RATIO, MAX_IMG_RATIO` |
| 그림 렌더링 해상도 | 200dpi | `scripts/figures.py: DPI` |
| 그림 좌우 여백 | 14pt | `scripts/figures.py: FIG_PAD` |
| 그림 최소 높이 | 40pt (미만이면 자르지 않음) | `scripts/figures.py: MIN_HEIGHT` |
| 본문 판정 연속 줄 수 | 3줄 (표 머리행이 본문 크기·폭이라 한두 줄로는 못 가른다) | `scripts/figures.py: PROSE_RUN` |
| 캡션 아래 영역 한계 | 페이지 높이의 85% (전면 표) | `scripts/figures.py: MAX_BELOW_RATIO` |
| 전면 배경 판정 | 페이지 면적의 60% 초과인 도형·이미지는 그림이 아니라 배경 | `scripts/figures.py: MAX_GRAPHIC_RATIO` |
| 그림 글자 묶음 간격 | 40pt 안에 닿는 글자까지 같은 그림으로 본다 | `scripts/figures.py: GROW_GAP` |
| 본문 크기 추가 인정 | 전체 줄의 20% 이상을 차지하는 크기는 본문으로 인정(중앙값의 1.20배까지) | `scripts/figures.py: COMMON_SIZE_RATIO, COMMON_SIZE_MAX` |
| 제목 판정 문자 비율 | 공백 뺀 글자의 70% 이상이 문자여야 제목 (스캔 잔재 배제) | `scripts/post.py: LETTER_RATIO` |
| 반복 요소 판정 | 3쪽 이상에 등장 | `scripts/post.py: repeated_chars(min_pages=)` |
| 청크 크기 | 6,000자 | `scripts/chunk.py: --max` |
| 제목 판정 | 본문 중앙값의 1.08배(맥락 조건부)·1.25배(무조건) | `scripts/post.py: HEADING_MIN, HEADING_STRONG` |
| 문단 들여쓰기 | 단 기준선 + 6pt | `scripts/post.py: INDENT_PT` |
| 인용 블록 들여쓰기 허용차 | 3pt (스캔본은 같은 인용문 안에서 x0가 3.6pt까지 흔들린다) | `scripts/post.py: QUOTE_INDENT_TOL` |
| 추출기 옵션 | `--no-tables` (표 있는 문서만 예외) | `scripts/kordoc.py: to_markdown` |
| 연표 항목 제외 | 코드+날짜(`B1 10/83` 등)가 2회 이상 포함된 문단은 절단 검사 대상에서 뺀다 | `scripts/quality.py: _CHRONOLOGY` |
| 수식 도입 문단 제외 | display equation을 도입하는 문단(`…formulated as`, `…following` 등)은 절단이 아니다 | `scripts/quality.py: _EQ_INTRO` |

## 추출 경로는 셋 중에서 고른다

논문마다 잘 맞는 경로가 달라서, 사람이 고르지 않고 게이트가 점수로 고른다. `meta.json`의 `extractor`에 무엇이 쓰였는지 적힌다.

| 경로 | 성격 |
|---|---|
| `columns` | PyMuPDF 줄 좌표로 단을 직접 재구성. 혼합 레이아웃(전면 폭 제목 + 2단)에 강하다 |
| `kordoc` | KorDocAI CLI. 순수한 2단 페이지에 정확하지만 혼합 레이아웃에서 좌우 단을 합친다 |
| `pymupdf4llm` | 기존 경로. 마지막 폴백 |

`--no-tables`는 빼면 안 된다. 기본값에서는 2단 조판의 테두리를 표로 오인해 좌우 단이 한 줄에 섞인다.
표가 실제로 있는 문서(PyMuPDF가 감지)에서만 표 감지를 켠 후보를 추가로 돌린다.
표 자체는 PyMuPDF가 직접 마크다운으로 렌더링해 넣으므로 외부 도구의 표 감지에 기대지 않는다.

## 실측

| 논문 | 보존율 | 잘린 문단 | 그림·표 | 청크 | 번역 시간 |
|---|---|---|---|---|---|
| Peteraf 1993 (14쪽, 2단 스캔본) | 1.006 | 0.014 | 3개 | 12 | 약 40분 |
| Noda·Bower 2007 (35쪽, 13만 자) | 0.996 | 0.016 | 7개 | 29 | 53분 |
| Teece 2007 (33쪽, 14.7만 자, 전면 다이어그램 스캔본) | 0.996 | 0.049 | 3개 | — | — |

Teece는 추출까지만 실측했다(번역 미실행). 개선 전 잘린 문단 0.131로 게이트에 걸려 번역이
시작되지 못했고, 원인과 수정 경위는
[`docs/superpowers/specs/2026-09-09-figure-region-and-quote-indent-design.md`](docs/superpowers/specs/2026-09-09-figure-region-and-quote-indent-design.md)에 있다.

개선 전 Peteraf는 보존율 0.686, 잘린 문단 0.903, JSTOR 푸터 14회 잔존, 각주가 본문에 혼입된
상태였다. 논문별 상세 경위와 각 논문이 드러낸 결함은 [`docs/실측기록.md`](docs/실측기록.md)에 있다.

## 처리 방침

- **각주**: 해당 페이지 끝으로 모아 `> **각주 N**` 형태로 표시하고 번역한다. 단 하단에 그대로 두면 본문 문단 한가운데를 갈라놓는다.
- **머리말·푸터·JSTOR 배너**: 삭제한다. 여러 쪽에 반복되는 줄을 `post.py`가 찾아 지우고, 표지 약관도 같이 버린다.
- **스캔 잔재**: 그림·로고의 OCR 잔재는 '큰 글자로 된 짧은 줄'이라 제목 조건을 그대로 만족한다(`?S.SZ o .2`, `Isl11?`, `C0 g`). 문자 비율이 낮거나 낱말을 이루지 못하는 줄은 제목으로 올리지 않는다.
- **쪽 번호**: 위·아래 여백(페이지 높이의 8% 바깥)에 숫자만 홀로 있는 줄은 버린다. 쪽마다 값이 달라 반복 판정으로는 안 걸리고, 남으면 본문보다 큰 글자라 제목으로 승격되어 문장을 가른다.
- **표지**: 서지 정보만 남기고 이용약관 문단은 버린다.
- **그림·표·연구모형**: 페이지에서 해당 영역을 200dpi로 잘라 이미지로 넣는다. 스캔본은 그림이 페이지 이미지의 일부라 따로 뽑을 수 없기 때문이다. 캡션(`Figure N.` / `Table N.`) 바로 앞에 이미지가 놓이고, 캡션은 텍스트로 남아 번역된다.
- **그림 안 글자**: 잘라낸 영역의 텍스트는 본문에서 뺀다. 대개 OCR 잔재(`p S2 ~~ ~ ~~~~~AC AC`)라 본문을 더럽힌다.
- **삽입 이미지**: 로고(면적 3% 미만)와 전면 스캔 배경(60% 초과)은 제외한다. 남은 그림은 HTML에 base64로 넣는다.
- **문장 잇기**: 페이지·단을 넘어 이어지는 문장은 한 문단으로 합친다. 끼어든 각주는 뒤로 옮긴다. 제목을 넘어서는 잇지 않는다.
- **캡션 방향**: 표 캡션은 표 위에, 그림 캡션은 그림 아래에 붙는다. 제 방향이 비어 있으면 반대쪽을 본다.
- **그림 영역 폭**: 캡션이 속한 단으로 잡은 뒤, 그 영역에 걸치는 도형·이미지의 실제 범위까지 넓힌다. 전면 다이어그램의 캡션은 그림보다 좁게 가운데 놓이므로 캡션 폭으로 자르면 그림의 반대쪽 절반이 본문으로 샌다. 넓힌 결과가 본문 문단을 삼키면 넓히지 않는다.
- **인용 블록**: 인터뷰 인용문처럼 모든 줄이 같은 위치로 들여쓴 덩어리는 첫 줄만 문단을 연다. 줄마다 끊으면 문장이 조각난다.
- **참고문헌**: 번역하지 않는다.
- **역어**: 하나만 쓴다. 원어 병기는 첫 등장 한 번만, 괄호 한 쌍만.

## 통독 단계에서 자동으로 고치는 것과 물어보는 것

| 자동 수정 | 사용자 확인 |
|---|---|
| 같은 용어가 앞뒤로 다르게 번역된 것 | 역어 선택이 갈리는 용어 |
| 청크 경계의 지시어 연결 | 원문 자체가 모호한 대목 |
| 번역투·어색한 문장 | 원문에 근거가 없어 판단이 필요한 곳 |

## 쓰는 법

```
논문번역기.bat 더블클릭  →  브라우저에서 PDF를 끌어다 놓는다
```

진행 상황이 막대로 표시되고, 끝나면 세 가지로 받을 수 있다.

| 버튼 | 받는 것 |
|---|---|
| 번역본 보기 | 그림이 박힌 HTML을 브라우저에서 연다 |
| PDF 내려받기 | A4 조판 PDF. 처음 누를 때 10초 안팎 걸려 굽고, 그다음부터는 즉시 받는다 |
| MD 내려받기 | 마크다운 원본 |

PDF는 브라우저 헤드리스 인쇄로 굽는다(Edge 또는 Chrome). HTML이 이미 A4 인쇄 CSS를 갖고
있어 화면과 같은 모양으로 나오고, 글자는 이미지가 아니라 선택·검색되는 텍스트로 남는다.
브라우저가 없으면 안내 문구가 뜨며, 그 경우 "번역본 보기"로 연 뒤 Ctrl+P → 'PDF로 저장'으로
받으면 된다.

## 결과를 받은 뒤 고치고 싶을 때

Claude에게 이렇게 말하면 된다. **번역을 처음부터 다시 하지 않는다.**

- "`fungible resource`를 `전용 가능 자원`으로 바꿔줘" → 용어표 갱신 후 전 문서 일괄 교체·재조립
- "3장 문체가 어색해" → 해당 청크만 다시 검수
- "각주를 문서 맨 끝으로 몰아줘" → 이 명령서의 처리 방침을 고치고 재조립

## 추출이 실패하면

`work/<제목>/extract_report.md`에 어느 문단이 잘렸는지 적혀 있다. 웹 UI의 "로그 보기"로도 볼 수 있다.
번역을 시작하지 않고 멈추는 것이 정상 동작이다 — 깨진 원문을 번역해봐야 쓸 수 없다.

후보 경로를 모두 돌려 점수가 가장 좋은 것을 쓰고, 그래도 미달이면 멈춘다. `meta.json`의 `extractor` 항목에 어느 경로가 쓰였는지 적힌다.

## 아직 안 켠 기능

필요해지면 말만 하면 된다. KorDocAI 옵션으로 바로 켤 수 있다.

- 수식이 많은 논문 → `--formula-ocr`
- 스캔본(텍스트 없는 PDF) → `--ocr`

## 확인하지 않고 넘긴 것

- **각주 위치 판정의 회귀 미확인 — 2026-09-09.** Bresnahan 1996(단단 책 챕터)에서 각주가
  본문으로 읽혀 문단 44개가 갈렸다(잘린 문단 0.174). 원인은 본문 8pt·각주 7pt인데 상한이
  6.8pt(`FOOTNOTE_RATIO` 0.85)라 0.2pt 차이로 빗나간 것. "본문 밴드보다 작고 그 페이지에서
  본문이 끝난 아래에 있으면 각주"로 바꿔 0.037로 통과시켰다.
  **속도 우선으로 Bresnahan만 재추출했고 나머지 4편은 확인하지 않았다.** 이 수정으로 각주
  판정이 바뀌는 줄은 Teece 95줄, Noda 31줄, Peteraf 12줄로 실측됐다. 이미 번역이 끝난
  3편(Mintzberg·Noda·Peteraf)과 Teece를 다시 돌릴 때는 지표를 먼저 확인할 것.
  기준선: Mintzberg 1.008/0.031, Noda 0.996/0.016, Peteraf 1.006/0.014, Teece 0.996/0.049.

## 불편사항 기록

쓰다가 걸린 점을 아래에 한 줄씩 적어둔다. 다음 개선 작업의 입력이 된다.

- (예시) 표가 있는 논문에서 표 안 문장이 잘렸다 — 2026-09-04
-
